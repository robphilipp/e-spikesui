// add a code snippet to simulate the sensors that sends signals to the
// spikes neural network. The code snippet must return an rx-js Observable which emits
// SensorOutput (the sensor output has the following shape:
// {
//     sensorName: string;
//     neuronIds: Array<string>;
//     signal: {
//         units: "µV" | "mV",
//         value: number
//     };
// })
// See https://rxjs-dev.firebaseapp.com/guide/observable
return interval(100).pipe(
   filter(t => t % 3 === 0),
   map(t => `2 x ${t} = ${2 * t}`)
)
