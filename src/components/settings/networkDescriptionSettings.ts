/**
 * Settings about network description files
 */
export interface NetworkDescriptionSettings {
    // the path to the default network description template that is used when
    // a creating a new network description
    templatePath: string;
}
