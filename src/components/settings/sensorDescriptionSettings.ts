/**
 * Settings about sensor code-snippet files
 */
export interface SensorDescriptionSettings {
    // the path to the default environment code-snippet template that is used when
    // a creating a new environment code-snippet
    templatePath: string;
}
