/**
 * Holds the path to the template files for the network description and the environment
 */
export default interface TemplateSettings {
    networkDescriptionPath: string;
    sensorDescriptionPath: string;
}